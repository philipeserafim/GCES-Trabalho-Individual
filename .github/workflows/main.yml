name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, devel]
    
defaults:
  run:
    working-directory: ./
    
jobs:
  build_app: 
    name: Build_app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build_App
        run: docker-compose run library_db library_back

  test_app:
      name: Test_app
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: Test_App
          run: docker-compose run library_db library_back bash -c "python manage.py test"
